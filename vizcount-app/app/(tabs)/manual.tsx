import { View, Text } from 'react-native';

export default function ManualScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-brand-dark">
            <Text className="text-brand-text text-xl font-bold">Manual Entry</Text>
        </View>
    );
}
