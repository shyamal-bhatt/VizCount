import { View, Text } from 'react-native';

export default function ExpiryScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-brand-dark">
            <Text className="text-brand-text text-xl font-bold">Check Expiry</Text>
        </View>
    );
}
