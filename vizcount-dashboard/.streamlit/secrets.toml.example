# Copy this file to .streamlit/secrets.toml and fill in your credentials.
# secrets.toml is gitignored — never commit real credentials.
#
# When deployed on Snowflake, these are managed via Snowflake's Secrets Manager
# and injected automatically — no secrets.toml needed in production.

[connections.vizcount_dashboard]
account   = "TBLRCRX-TRC13787"
user      = "YOUR_SNOWFLAKE_USER"
password  = "YOUR_SNOWFLAKE_PASSWORD"
role      = "ACCOUNTADMIN"
warehouse = "COMPUTE_WH"
database  = "VIZCOUNT_DB"
schema    = "PUBLIC"
host      = "suc20429.us-east-1.snowflakecomputing.com"
